# Google Apps Script — versionado no repositório (fonte da verdade).
# Editar aqui e copiar de volta para o script.google.com após cada mudança.
# bot_tests.gs segue fora por enquanto (descomente para versionar também).
bot_tests.gs

# Instruções internas — não precisam ir ao repositório público
instrucoes_planilha.md
instrucoes_slides.md
